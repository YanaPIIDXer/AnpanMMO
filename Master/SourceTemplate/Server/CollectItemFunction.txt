std::vector<const $ITEM_STRUCT_NAME$ *> $CLASS_NAME$::CollectItems($KEY_TYPE$ Key, s32 SheetIndex) const
{
	std::vector<const $ITEM_STRUCT_NAME$ *> Result;
	for(SheetMap::const_iterator It = Items.begin(); It != Items.end(); ++It)
	{
		for(ItemMap::const_iterator It2 = It->second.begin(); It2 != It->second.end(); ++It2)
		{
			if(It2->second.$KEY_NAME$ == Key)
			{
				Result.push_back(&It2->second);
			}
		}
	}
	return Result;
}